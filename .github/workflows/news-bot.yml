name: AI News Bot

on:
  schedule:
    # Har 20 daqiqada ishga tushadi (UTC bo'yicha)
    - cron: "*/20 * * * *"
  workflow_dispatch: {}   # GitHub'da "Run workflow" tugmasi bilan qo'lda ham ishga tushirish mumkin

# Bot o'zgargan fayllarni (posted_ids.json) repo'ga qaytarib yozishi uchun ruxsat
permissions:
  contents: write

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Kodni olish
        uses: actions/checkout@v4

      - name: Python o'rnatish
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Kutubxonalarni o'rnatish
        run: pip install -r requirements.txt

      - name: Botni ishga tushirish
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python main.py

      - name: O'zgarishlarni saqlash (posted_ids.json)
        run: |
          git config user.name "news-bot"
          git config user.email "news-bot@users.noreply.github.com"
          git add posted_ids.json
          git diff --quiet && git diff --staged --quiet || git commit -m "update posted ids"
          git push
